package com.companyname.hearts.model;

/*
    This enum represents the four types of direction that the game could encounter during passing
 */

public enum Direction {
    LEFT, RIGHT, ACROSS, NO_PASSING;
}
